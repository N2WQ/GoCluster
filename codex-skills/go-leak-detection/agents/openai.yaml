interface:
  display_name: "Go Leak Detection"
  short_description: "Investigate leaks with labeled and captured evidence"
  default_prompt: "Investigate leak risk in the Go code path using static reasoning, lifecycle tests, race checks, pprof, trace, retained-state evidence, and runtime profile captures; distinguish local evidence from runtime confirmation and require captured excerpts for command-backed claims."
